import React from 'react';
import { expect, assert } from 'chai';
import { spy, stub } from 'sinon';
import { shallow } from 'enzyme';

describe('SearchResult', () => {

  it('should fail', function() {
    assert.fail('so finish this test!');
  });

});
